//write a simple queue where you can add to a queue and call the Queue byit's key

//1. create an empty object
//2. create a function that adds requested name to the object


var queue = {

}

class Person {
  constructor(name, age, in) {
    
    this.name=name;
    this.age = age;

}
}

function askName(info) {
  var fullName = prompt("what's your name?");
  var age = prompt("what's your age?")

  return info = new Person(fullName, age )
}
function addtoQ() {
  return queue.info
}

// var Remi = new Person("Oluremi", "Bello", 19)

// console.log(Remi.first);